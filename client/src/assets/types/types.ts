export interface product {
  products: {
    productList: {
      ageMin: string;
      alt: string;
      brand: string;
      description: string;
      name: string;
      price: number;
      src: string;
      _id: string;
    };
  };
}
